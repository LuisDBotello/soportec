package soportec.demo.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import soportec.demo.models.CatTipoRed;

public interface CatTipoRedRepository extends JpaRepository<CatTipoRed, Integer> {
}
